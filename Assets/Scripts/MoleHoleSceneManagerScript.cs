using UnityEngine;

public class MoleHoleSceneManagerScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void PlayerDeath()
    {
        //each time you pick up an orb, add it to playerPrefs and update the text.
        //have a start of level number

        //revert the playerprefs orbs gathered number back to the number at the beginning
    }
}
